global:
  scrape_interval: 15s

scrape_configs:
  - job_name: whats-saas-health
    metrics_path: /api/health
    scheme: https
    static_configs:
      - targets: ['your-domain.com']
    # For full metrics, add prom-client exporter to Next.js (future)
